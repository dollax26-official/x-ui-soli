# x-ui on Railway â€” one port, panel at root, multi-node (central panel + nodes)\n\n[ÙØ§Ø±Ø³ÛŒ](./README.fa.md) | English\n\nRuns the [3x-ui](https://github.com/mhsanaei/3x-ui) panel (v3.8.5) behind an nginx\nreverse proxy on a **single Railway port**. The panel is served at the **root**\nof your domain (`https://<domain>/`), the VPN inbound keeps a catch-all so any\nWebSocket path works, and several deployments of this repo can be linked as a\n**central panel + nodes** using 3x-ui's built-in multi-node feature.\n\n**The panel itself is not modified** â€” only the nginx/startup wrapper changes.\n\n## Files\n\n| File | Role |\n| ---- | ---- |\n| `Dockerfile` | Builds the image (3x-ui + nginx + helper). |\n| `nginx.conf.template` | Single-port routing: panel at `/`, inbound catch-all. |\n| `start.sh` | Starts the panel; sets web base path to `/`; nginx listens on `$PORT`. |\n| `node-link.sh` | Mints a node API token and prints the values to paste into the central panel. |\n\n---\n\n## 1. Deploy\n\n1. Create a repo from these files (or deploy this repo directly).\n2. Railway â†’ **New Project â†’ Deploy from GitHub repo**. Railway detects the\n   `Dockerfile` and builds.\n3. **Settings â†’ Networking â†’ Generate Domain** for every service.\n4. *(Recommended)* Add a **Volume** at `/etc/x-ui`, otherwise users/inbounds/\n   settings/tokens are wiped on every redeploy.\n\n### Panel\n\n```\nhttps://<your-domain>.up.railway.app/\n```\n\nDefault `admin` / `admin` â€” change it in **Settings â†’ Panel**.\n\n### Inbound\n\nCreate a **VLESS** inbound with **Listen Port `8080`** (nginx forwards the\ncatch-all to it), Network **ws**, Security **none**, path e.g. **`/cdn`**.\n\n> **Reserved paths** (used by the panel, don't use them as the inbound path):\n> `/`, `/panel/`, `/assets/`, `/icons/`, `/login`, `/logout`, `/csrf-token`,\n> `/getTwoFactorEnable`, `/ws`, `/sub/`, and the PWA files. Any **other** path\n> (like `/cdn`) reaches the inbound, exactly as before.\n\n```\nvless://<UUID>@<your-domain>.up.railway.app:443?encryption=none&security=tls&sni=<your-domain>.up.railway.app&fp=chrome&type=ws&host=<your-domain>.up.railway.app&path=%2Fcdn#MyConfig\n```\n\n---\n\n## 2. Multi-node (connect the panels)\n\n```\n                  â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”\n   Clients â”€â”€â”€â”€â”€â–º â”‚  Central panel (master)        â”‚\n                  â”‚  https://central.up.railway.app â”‚\n                  â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¬â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜\n                                 â”‚ HTTPS + API token\n              â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”¼â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”\n              â–¼                  â–¼                  â–¼\n       â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”     â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”     â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”\n       â”‚  Node A    â”‚     â”‚  Node B    â”‚     â”‚  Node C    â”‚\n       â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜     â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜     â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜\n```\n\nThe central panel calls each node's panel API at\n`<scheme>://<address>:<port><basePath>panel/api/...`. Because the panel now lives\nat the **root** (base path `/`), a node connects with the **default** Base path â€”\nno `/managepanel/` to remember:\n\n| Add-node field | Value for a Railway node |\n| -------------- | ------------------------ |\n| **Name** | anything, e.g. `de-fra-1` |\n| **Scheme** | `https` |\n| **Address** | the node's Railway domain, e.g. `node-xxx.up.railway.app` (**no** `https://`) |\n| **Port** | `443` |\n| **Base path** | `/` *(leave the default)* |\n| **API token** | a token created **on the node** (Step A) |\n| **TLS verify** | `verify` |\n| **Inbound sync** | `all` (or `selected`) |\n\n### Step A â€” on the NODE: create the API key\n\nOpen the node's panel at `https://<node-domain>/`, then either:\n\n- **UI:** *Settings â†’ API Tokens â†’ create* (scope `admin`, or `node-sync`).\n  **Copy it now** â€” it is shown only once.\n- **Helper:** in the service shell run `bash /node-link.sh`. It mints a token and\n  prints a paste-ready block (saved to `/etc/x-ui/node-connection.txt`):\n\n  ```\n  Scheme:    https\n  Address:   node-xxx.up.railway.app\n  Port:      443\n  Base path: /              <- leave the default\n  API token: <the token>\n  ```\n\n### Step B â€” on the CENTRAL panel: paste the token + address\n\nOpen the central panel â†’ **Nodes â†’ Add node**, fill the table above (just enter\nthe **Address**; **Base path** can stay at `/`), and click **Test / Add**. When\nthe node shows **online**, the central panel reports its version, CPU/RAM, uptime\nand traffic, and pushes inbound/client edits to it.\n\n### Optional: mutual TLS instead of a token\n\nSet the node's **TLS verify** to `mtls`, copy the central panel's node CA\n(Nodes â†’ *Get CA*) into the node's *trusted CA* setting, and restart the node.\n\n---\n\n## 3. Environment variables\n\n| Variable | Default | Purpose |\n| -------- | ------- | ------- |\n| `PORT` | `3000` | Public port nginx listens on (set by Railway). |\n| `NODE_PUBLIC_ADDRESS` | `$RAILWAY_PUBLIC_DOMAIN` | Address printed by `node-link.sh`. |\n| `NODE_TOKEN_NAME` | `central` | Token name minted by `node-link.sh`. |\n\nIf Railway doesn't inject `PORT`, set the service target port to `3000`.\n\n---\n\n## 4. What changed vs. the original wrapper\n\nOnly reachability + node linking. **The panel is untouched.**\n\n- `start.sh` â€” panel web base path is now `/` (was `/managepanel/`); nginx binds\n  Railway's `$PORT` (`${PORT:-3000}`) instead of a fixed 3000.\n- `nginx.conf.template` â€” panel served at the root (`/`, `/panel/`, `/assets/`,\n  `/icons/`, `/login`, `/ws`, â€¦); the inbound stays a catch-all on port 8080.\n- `Dockerfile` â€” also copies `node-link.sh`.\n- `node-link.sh` â€” new, optional helper.\n\nEverything else (ports 2053/2096/8080, inbound behaviour) is unchanged.\n\n---\n\n## 5. Troubleshooting\n\n- **Panel doesn't open at `/`** â†’ make sure the deploy picked up the new files\n  and redeployed; the panel logs should say `base path: /`.\n- **My inbound stopped working** â†’ its path collides with a reserved panel path\n  (see the list above). Use something like `/cdn`.\n- **Node offline** â†’ Address has no scheme; Port `443`; Base path `/`; token\n  copied in full; node has a public domain.\n- **Token broke after redeploy** â†’ no `/etc/x-ui` volume, so the node DB (and its\n  token) was reset. Mount the volume, or mint a new token.\n